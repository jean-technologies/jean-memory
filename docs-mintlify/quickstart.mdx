---
title: Quickstart
description: 'Get running in 2 minutes'
---

## How Jean Memory Works

<div style={{textAlign: 'center', margin: '2rem 0'}}>
  <div style={{display: 'flex', alignItems: 'center', justifyContent: 'center', gap: '2rem', flexWrap: 'wrap'}}>
    <div style={{padding: '1.5rem', borderRadius: '8px', background: 'var(--tw-prose-pre-bg)', border: '1px solid var(--tw-prose-th-borders)', minWidth: '120px', textAlign: 'center'}}>
      <strong>Your App</strong>
      <div style={{fontSize: '0.875rem', marginTop: '0.5rem', opacity: 0.8}}>Any UI</div>
    </div>
    <div style={{fontSize: '1.5rem'}}>→</div>
    <div style={{padding: '1.5rem', borderRadius: '8px', background: 'linear-gradient(135deg, #9563FF 0%, #7C3AED 100%)', color: 'white', minWidth: '120px', textAlign: 'center'}}>
      <strong>Jean Memory</strong>
      <div style={{fontSize: '0.875rem', marginTop: '0.5rem', opacity: 0.9}}>Memory Layer</div>
    </div>
    <div style={{fontSize: '1.5rem'}}>→</div>
    <div style={{padding: '1.5rem', borderRadius: '8px', background: 'var(--tw-prose-pre-bg)', border: '1px solid var(--tw-prose-th-borders)', minWidth: '120px', textAlign: 'center'}}>
      <strong>AI Model</strong>
      <div style={{fontSize: '0.875rem', marginTop: '0.5rem', opacity: 0.8}}>Enhanced</div>
    </div>
  </div>
  <p style={{marginTop: '1.5rem', fontSize: '0.875rem', opacity: 0.8}}>Jean Memory intelligently orchestrates memory storage, retrieval, and deduplication in the background</p>
</div>

<iframe 
    width="100%" 
    style={{aspectRatio: "16 / 9"}} 
    src="https://www.youtube.com/embed/kO6f7doPjSI" 
    title="YouTube video player" 
    frameBorder="0" 
    allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture" 
    allowFullScreen>
</iframe>

## 1. Get Your API Key

Sign up at [jeanmemory.com](https://jeanmemory.com) and grab your API key.

## 2. Install

<Tabs>
  <Tab title="React">
    ```bash
    npm install @jeanmemory/react
    ```
  </Tab>
  <Tab title="Python">
    ```bash
    pip install jeanmemory
    ```
  </Tab>
  <Tab title="Node.js">
    ```bash
    npm install @jeanmemory/node
    ```
  </Tab>
</Tabs>

## 3. Add to Your App

<Tabs>
  <Tab title="React">
    ```javascript
    import { useJeanAgent, JeanChat } from '@jeanmemory/react';
    
    function App() {
      const { agent } = useJeanAgent({ 
        apiKey: process.env.REACT_APP_JEAN_API_KEY 
      });
      return <JeanChat agent={agent} />;
    }
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from jeanmemory import JeanAgent
    import os
    
    agent = JeanAgent(
        api_key=os.getenv("JEAN_API_KEY")
    )
    agent.run()
    ```
  </Tab>
  <Tab title="Node.js">
    ```javascript
    import { JeanAgent } from '@jeanmemory/node';
    
    const agent = new JeanAgent({
      apiKey: process.env.JEAN_API_KEY
    });
    
    await agent.run();
    ```
  </Tab>
</Tabs>

## 4. Set Environment Variable

```bash
# .env file
JEAN_API_KEY=jean_sk_your_api_key_here
```

## That's It!

Your AI now has persistent memory. Test it:

1. Tell it something: "My favorite color is blue"
2. Ask: "What's my favorite color?"

It remembers across sessions automatically!

## Sign In With Jean

<div style={{padding: '2rem', borderRadius: '8px', background: 'var(--tw-prose-pre-bg)', border: '1px solid var(--tw-prose-th-borders)', margin: '2rem 0'}}>
  <div style={{display: 'flex', alignItems: 'center', justifyContent: 'center', marginBottom: '1rem'}}>
    <div style={{padding: '0.75rem 1.5rem', borderRadius: '6px', background: 'linear-gradient(135deg, #9563FF 0%, #7C3AED 100%)', color: 'white', fontWeight: 'bold', fontSize: '0.875rem'}}>
      Sign In with Jean
    </div>
  </div>
  <p style={{textAlign: 'center', margin: 0, fontSize: '0.875rem', opacity: 0.8}}>Businesses can customize this however they'd like:</p>
  <div style={{display: 'flex', gap: '0.5rem', justifyContent: 'center', flexWrap: 'wrap', marginTop: '1rem'}}>
    <code style={{fontSize: '0.75rem', padding: '0.25rem 0.5rem'}}>"Add Memory to X"</code>
    <code style={{fontSize: '0.75rem', padding: '0.25rem 0.5rem'}}>"Bring Your Memories"</code>
    <code style={{fontSize: '0.75rem', padding: '0.25rem 0.5rem'}}>"Personalize"</code>
  </div>
</div>
